# Design QA

## 2026-07-13 节点与关系线边缘同步渐隐验收

- 本机 Chrome 页面：`http://127.0.0.1:8765/?edge-fade=20260713-11`
- 浏览器截图：`C:\Users\33084\Desktop\唐玉超老师的文章\research-timeline\docs\qa-relation-edge-fade.png`
- 视口与状态：桌面端、全部方向、关系线开启，通过底部密度导航将年份视图由 2009–2029 横移至 2016–2036，等价验证横向拖拽后的渲染边界。
- 浏览器证据：横移前渲染 85 个节点和 48 条关系线；横移后仅渲染可见范围内的 65 个节点和 37 条关系线。
- 同步证据：边缘节点最小透明度与关系线最小透明度均为 `0.9374157550`；DOM 中不存在透明度小于等于 0 但仍保留的节点或关系线。
- 字体、颜色、文案、图片资源和页面布局未改变；仅调整节点与关系线的边缘透明度与渲染裁剪。
- Findings：未发现 P0、P1 或 P2 问题；截图中没有连接到已隐藏端点的残留曲线。
- Comparison History：初始代码检查发现节点有边界裁剪而关系线没有；共享透明度修复后首次 Chrome 对比通过，无后续视觉修复轮次。
- 控制台检查：页面正常加载和交互，未观察到项目脚本错误。

final result: passed

## 2026-07-13 节点点击命中与拖拽阈值（本地未提交）

- 根因：可见 SVG 节点外圈多数为空心，边缘指针会穿透到画布；画布又在按下时立即进入拖拽，造成 pointer/grab 与点击/拖动反复切换。
- 节点命中：Chrome 实测小节点可见图形约 `12.4px`，透明命中圈统一至少 `28px`，视觉尺寸不变。
- 边缘点击：在 A90 可见图形外、透明命中圈内点击，成功选中 A90 并打开详情；纵向滚动未变化，没有触发画布拖拽。
- 轻微抖动：空白区域横向移动 `4px` 后，底部视窗位置与 `scrollTop` 均保持不变，拖拽状态未启动。
- 万向拖拽：放大后从空白区域斜向拖动，底部视窗左边从 `456.21px` 变为 `413.52px`，`scrollTop` 从 `76.8px` 变为 `136.8px`。
- 光标：空白画布默认使用普通箭头，仅超过 `5px` 阈值的真实拖拽期间显示抓手；节点保持 pointer。
- 默认状态：验收结束后详情关闭、无选中节点、86 个节点恢复显示。
- 控制台：未发现页面脚本错误或警告。
- 验收截图：`docs/qa-node-hit-target.png`。

final result: passed

## 2026-07-13 创新性筛选与同期数量提示（本地未提交）

- 左侧新增“创新性”筛选板块，按两列紧凑排列。
- 分类与数量：核心创新 1、显著扩展 27、增量改进 46、应用迁移 6、证据不足 6，共 86 篇。
- 创新性筛选可与方向、类型、代表作、搜索和年份条件组合使用。
- Chrome 实测仅保留“核心创新”时结果为 1 篇；点击“全部”后恢复 86 篇、5 个分类全选。
- 节点右上角的 `+N` 统一改为真实的同期提示：同一研究方向、同一年另有 N 篇论文，所有节点均已显示。
- `+N` 图例固定在左下角；节点徽标和悬停提示均带有完整语义说明。
- 验收截图：`docs/qa-innovation-filter.png`。

final result: passed

## 2026-07-13 默认启动状态视觉验收

**对比证据**

- 视觉真值：`C:\Users\33084\AppData\Local\Temp\codex-clipboard-1c9e5bb4-683c-47e4-98ac-2338b3975bfa.png`
- Chrome 实现截图：`C:\Users\33084\Desktop\唐玉超老师的文章\research-timeline\docs\qa-default-state.png`
- 同屏对比图：`C:\Users\33084\Desktop\唐玉超老师的文章\research-timeline\docs\qa-default-state-comparison.png`
- 视口：参考图为 1920×867 物理像素；本机 Chrome 为 1536×639 CSS 像素，受 Windows 显示缩放影响。
- 状态：桌面端、全部方向、默认年份跨度 21 年、无文献详情、无选中节点。
- 全图对比：实现与参考图均显示 2009–2029 年，2026 年竖线对准右下角浮动工具栏左边缘，六个研究方向保持在首屏结构中。
- 局部证据：Chrome DOM 几何测量中，2026 年竖线中心为 `1317.94px`，浮动工具栏左边缘为 `1318.20px`，误差 `0.26px`；精确测量和同屏全图已足以判断该局部，无需额外裁切图。

**Findings**

- 未发现与本次默认启动状态相关的 P0、P1 或 P2 问题。
- 字体与排版：沿用现有字体层级和字号，没有因默认视图调整产生换行或截断漂移。
- 间距与布局：年份跨度保持 21 年，目标竖线与工具栏对齐误差低于 1px。
- 颜色与视觉令牌：没有修改现有方向色、背景色、边框或阴影令牌。
- 图片与资源：本次未引入图片、图标或替代资源。
- 文案与内容：未改变论文数据和界面文案，仅补充项目说明。

**交互与运行检查**

- 首次打开时 `.workspace` 包含 `detail-closed`，详情面板为 `display: none`，选中节点为 `null`。
- 点击论文 A31 后详情正常打开且选中 A31；同节点再次关闭逻辑由回归测试覆盖。
- 本机 Chrome 加载、默认状态与首次点击过程中未观察到页面自身报错；此前记录的扩展上下文报错不属于本项目。

**Comparison History**

- 首次正式对比未发现可执行的 P0、P1 或 P2 差异，因此没有因视觉对比产生后续修复轮次。

final result: passed

## 对比基线

- 视觉参考：Stitch 导出 `screen.png`。
- 实现页面：Chrome 中的 `http://127.0.0.1:8765/`。
- 对比状态：桌面首屏、全部 86 篇论文、2006–2026、六个方向、右侧默认展示最新代表作。

## 迭代记录

1. 初次实现完整呈现 86 个节点，但 37 个标题卡片在 2022–2026 高密度区发生遮挡。
2. 将标题卡片改为按方向和双年窗口择优显示，保留全部节点及悬停信息，首屏卡片减少到 16 个。
3. 修复 `hidden` 属性被详情空状态样式覆盖的问题，详情内容回到抽屉顶部。
4. 默认加载最新代表作详情，同时只在用户主动点击节点后淡化无关节点，避免首屏灰暗。
5. 添加静态资源版本参数，防止 Cloudflare 或浏览器继续使用旧 CSS/JS。

## 最终检查

- 结构：顶部工具栏、左侧筛选、中央六泳道、右侧详情、底部密度导航与 Stitch 基线一致。
- 数据：86 篇论文、27 篇代表作、6 个方向、36 条关系全部来自真实 CSV/JSON。
- 可读性：论文卡片无大面积遮挡，节点形状、方向颜色和代表作环清晰可辨。
- 交互：搜索、组合筛选、重置、节点详情、关系高亮、缩放、平移、导出和移动端筛选抽屉均通过 Chrome 验证。
- 响应式：390×844 窄屏无页面横向溢出，侧栏以抽屉方式打开。
- 结论：通过。

## 2026-07-13 交互微调（本地未提交）

- 工具栏：已移动至主画布右下角并改为横向排列。
- 泳道：当前六个方向高度为 `134 / 138 / 122 / 112 / 74 / 80px`，稀疏方向不再与高密度方向等高。
- 二维缩放：Chrome 实测 SVG 高度由 `698px` 放大至 `929px`，横向年份视窗同步收窄。
- 万向拖拽：Chrome 实测一次斜向拖拽使纵向 `scrollTop` 从 `77.6` 变为 `137.6`，横向年份位置同步变化。
- 默认标签：首屏论文标题卡片数为 `0`；主动选择后为 `1`。
- 详情切换：重复点击同一论文后标题卡片归零、详情面板隐藏，主画布自动扩宽。
- 固定头部：详情面板滚动至 `175.2px` 后，头部仍保持在视口 `top: 60px`，关闭按钮持续可见。
## 2026-07-13 第二轮交互微调（本地未提交）

- 滚轮：移除泳道画布的滚轮缩放监听；Chrome 实测滚轮后 `scrollTop` 从 `76.8` 变为 `142.4`，SVG 高度与横向年份视窗宽度保持不变。
- 缩放：仅右下角缩放按钮改变泳道长宽；“适应视图”会恢复六泳道完整显示。
- 初始视图：在 `1536×639` 浏览器视口下，画布可视高度与内容高度均为 `547px`，6 条泳道完整显示，最后一条泳道底部为 `543px`。
- 节点尺寸：初始视图节点实测尺寸约为 `12.4–19.0px`，保持可辨识且不挤压泳道。
- 侧栏：图例固定在左下角，侧栏内容独立滚动；滚动到底后图例位置不变，文献类型板块完整显示在图例上方。
- 控制台：未发现页面自身脚本错误；现有报错均来自 Chrome 扩展上下文，与本项目无关。
## 2026-07-13 文献位置与关系重建（本地未提交）

- 数据规模保持 86 篇、2006–2026 年不变。
- 24 篇调整主泳道，36 篇调整文献类型。
- 演化关系由 36 条重建为 50 条，删除 20 条倒置或证据不足的旧边，新增 34 条经本地研究梳理支持的边。
- 自动校验覆盖 ID、方向、类型、父 ID、重复边、自环和未来父文献。
- Chrome 实测完整渲染 86 个论文节点、6 条泳道和 50 条关系线；各泳道计数为 `22 / 15 / 20 / 19 / 2 / 8`。
- 关键位置实测：A20/A31 位于单调包含泳道，A33/A62/A82/A86/A87 位于不动点泳道，A37 位于原始对偶泳道，A14 位于交叉计算泳道。
- 详情关系实测：A3 只显示从 A11 继承；A31 显示从 A38、A45 继承并延伸至 A19。
- 初始画布可视高度与内容高度均为 `547px`，重分类后仍完整显示六条泳道。

## 2026-07-13 Grok 4.5 论文精读与创新性面板（本地未提交）

- 数据覆盖：86/86 篇，81 篇基于 MinerU 全文，5 篇仅基于摘要；生成模型统一为 `grok-4.5`。
- Chrome 验收页：`http://127.0.0.1:8765/?insights=20260713-12`。
- 验收截图：`docs/qa-paper-insights.png`。
- 默认状态：详情面板关闭、无选中节点，保持既定的 2026 年对齐视图。
- 全文样本 A31：显示“显著扩展”“全文精读”“4/5”“置信度 86%”，4 个精读折叠区中仅“创新从哪里来”默认展开，3 条演化关系仍可用。
- 摘要样本 A3：显示“摘要判断”“置信度 55%”，证据边界正常呈现，不把摘要信息包装成全文结论。
- 信息层级：创新结论卡优先展示，继承基础、增量推进、核心创新、理论保证、实验、局限与研究谱系按需展开，避免详情面板成为长篇信息墙。
- 视觉检查：沿用现有蓝灰色令牌、宋体标题层级、边框半径与紧凑间距；未发现内容溢出、异常裁切或控件遮挡。
- 交互检查：文献切换、相同节点关闭逻辑、固定详情头部和关闭按钮均保留；最终已恢复默认关闭状态。
- 控制台：未发现页面脚本错误或警告。

final result: passed
